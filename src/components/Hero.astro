<script is:inline>
	window.addEventListener('load', ()=>{
		video.play()
	})
</script>

<section class='h-[45vh] md:h-[60vh] w-full flex items-center'>
	<div
		class='relative container w-full mx-auto flex justify-around content-center'
	>
		<video id="video" src='/video.mp4' controls></video>
	</div>
</section>

<style>
	div::before {
		content: '';
		background: rgba(165, 165, 165, 0.4);
		border-radius: 50%;
		filter: blur(100px);
		width: 300px;
		height: 300px;
		position: absolute;
		left: 300px;
		top: 150px;
		z-index: -1;
	}
	div::after {
		content: '';
		background: rgba(165, 165, 165, 0.2);
		border-radius: 50%;
		filter: blur(90px);
		width: 300px;
		height: 300px;
		position: absolute;
		right: 300px;
		bottom: 70px;
		z-index: -1;
	}

	@keyframes gradation {
		from {
			filter: hue-rotate(0deg);
		}
		to {
			filter: hue-rotate(-360deg);
		}
	}

	@media (max-width: 1020px) {
		.textwrapper > h2 {
			font-size: 3rem;
		}
	}
	@media (max-width: 630px) {
		.textwrapper > h2 {
			font-size: 2.7rem;
		}
		section::before {
			width: 150px;
			height: 150px;
			left: 50px;
			top: 100px;
		}
		div::after {
			display: none;
		}
		div::before {
			content: '';
			background: rgba(165, 165, 165, 0.4);
			border-radius: 50%;
			filter: blur(100px);
			width: 300px;
			height: 300px;
			position: absolute;
			left: 30px;
			top: 0px;
			z-index: -1;
		}
	}
</style>
